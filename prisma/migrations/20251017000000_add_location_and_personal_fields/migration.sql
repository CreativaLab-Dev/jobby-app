-- AlterTable: Add address and linkedin to cv table (already applied via db push)
-- These changes are already in the database, this migration serves as a record

-- CV table already has:
-- address String?
-- linkedin String?

-- Experience table already has:
-- position String?
-- location String?

-- Education table already has:
-- location String?
-- year String?
-- honors String?

-- This migration is empty because changes were applied via prisma db push
